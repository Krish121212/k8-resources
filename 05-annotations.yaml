kind: Pod
apiVersion: v1
metadata:
  name: annotations
  labels:
    Technology: K8
    Environment: dev
  annotations:
    kubernetes.io/created-by: 'KK'
    jenkins.build.url: 'https://jenkins.devopskk.online/expense/job/backend/54'
spec:
  containers:
  - name: nginx
    image: nginx

# To create the external resources, i.e outside of K8,  
# Controllers and external tools can read annotations and act based on their values
# Also they allow special characters and Can store complex, multi-line data.
